define ["coffeequate"], (coffeequate) ->

  it "Coffeequate can be loaded", ->
    # expect(coffeequate.
    #     parse.stringToExpression("x+y").sub({"x":4, "y":45}).
    #     simplify().toString()).toEqual("49")
    expect(1).toEqual(1)